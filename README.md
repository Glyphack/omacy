# Omacy

Omacy is an opinionated mac setup automation script with one click install.

It runs on Apple silicon Macs only.

Package management is done with brew.
Shell is fish.


```
curl -fsSL https://raw.githubusercontent.com/Glyphack/omacy/HEAD/install.sh | bash
```
